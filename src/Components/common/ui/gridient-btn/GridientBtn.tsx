import "./GridientBtn.css";

const GridientBtn = ({ children }: { children: React.ReactNode }) => {
  return <button className="gradient-btn-class">{children}</button>;
};

export default GridientBtn;
